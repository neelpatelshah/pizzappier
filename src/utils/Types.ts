export type IOption = {
    label: string,
    value: string
}

export type IRecipe = {
    type: string,
    balls: number,
    weight: number,
    water: number,
    salt: number,
    sugar: number,
    oil: number,
    proof: number,
    temp: number,
    yeast: string,
    finalFlour: number,
    finalWater: number,
    finalSalt: number,
    finalSugar: number,
    finalOil: number,
    finalYeast: number
}